[project]
name = "originguard-model-worker"
version = "0.1.0"
description = "OriginGuard asynchronous model worker skeleton"
requires-python = ">=3.11,<3.12"
dependencies = ["pika>=1.3,<2", "pydantic>=2.11,<3"]

[project.optional-dependencies]
dev = ["pytest>=8.4,<9", "ruff>=0.12,<1", "mypy>=1.17,<2"]
